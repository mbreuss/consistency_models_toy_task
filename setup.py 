from setuptools import setup, find_packages

setup(
    name="consistency_model_toy_task",
    version="0.1",
    description="Unofficial minimal implementation of consistency models (CM) proposed by Song et al. 2023 on a 1D toy task in pytorch",
    license="MIT",
    author="Moritz Reus",
    url="https://github.com/mbreuss/consistency_model_toy_task",
    packages=find_packages(),
    install_requires=[
        "torch",
        "einops",
        "scikit-learn",
        "tqdm",
        "matplotlib",
        "numpy"
    ]
)
